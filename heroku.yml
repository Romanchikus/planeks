setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - python manage.py collectstatic --noinput
    # - python manage.py makemigrations users
    # - python manage.py makemigrations cert_app
    # - python manage.py migrate

run:
  web: gunicorn certificate.wsgi:application --bind 0.0.0.0:$PORT
